# psis_project
PSis Project

workflow
(changes and save add,commit)
git checkout master
git pull
git checkout <branch>
git merge master
(resolver conflitos)
git push
(pull request)(não deve haver conflitos)
